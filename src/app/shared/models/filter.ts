export class Filter {
    constructor(
        public name: string,
        public population: number,
        public households: number,
        public popDensity: number,
        public avgPowerUsage: number
    ) { }
}